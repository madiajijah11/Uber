import React from "react"; 
import { StyleSheet, Text, View, Button, Image, TouchableOpacity, ScrollView, FlatList, Dimensions, Alert, ActivityIndicator } from "react-native";

const HomeScreen = () => {
    return (
        <View>
            <Text>Home Screen</Text>
        </View>
    );
}

export default HomeScreen;