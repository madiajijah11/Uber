import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container : {
        padding: 10,
        height: "100%",
        backgroundColor: "#eee",
    },
    inputText : {
        padding: 10,
        backgroundColor: '#fff',
        marginVertical: 5,
    },
});

export default styles;